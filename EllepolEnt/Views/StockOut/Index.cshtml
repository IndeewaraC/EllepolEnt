@model IEnumerable<EllepolEnt.Models.StockOut>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.stockoutDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ItemId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Stockout)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StockoutPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.stockoutDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ItemId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Stockout)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StockoutPrice)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Stock_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Stock_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Stock_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
